<!DOCTYPE html>
<html>
<head>
  <title>PasswordVault</title>
  <link href="http://picnicss.com/releases/v2.3.min.css" rel="stylesheet" type="text/css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav>
  <%= link_to "Password Vault", root_path, class: 'main' %>
  
  <!-- responsive -->
  <input id="bmenu" class="burgercheck" type="checkbox">
  <label for="bmenu" class="burgermenu"></label>
  <!-- /responsive -->

  <div class="menu">
    <%= link_to "Passwords", passwords_path, class: 'icon-key' %>
    <%= link_to "Groups", groups_path, class: 'icon-suitcase' %>
    <%= link_to "Users", users_path, class: 'icon-users' %>
    <% if user_signed_in? %>
      <%= link_to current_user.name, current_user, class: 'icon-user' %>
      <%= link_to '', destroy_user_session_path, method: :delete, class: 'icon-logout' %>
    <% end %>
  </div>
</nav>

<div class="main-content">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>

</body>
</html>
